<%= form_tag(login_path, method: "post", :class=>"form-horizontal", :id=>"login_form") do %>

  <%= label :user, :username%>
  <%= text_field :user, :username, :class=>"form-control"%>
  <br/>
  <%= submit_tag "Sign in", class: "btn btn-large btn-primary" %>

<% end %>
